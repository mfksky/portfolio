---
const { variant } = Astro.props;
---

<div class={`blob blob-${variant}`}></div>

<style lang="scss">
    .blob {
        position: fixed;
        z-index: -1;
        width: 30rem;
        height: 30rem;
        background: linear-gradient(to right, #5e76fa, #c7c6d3);
        filter: blur(15rem);
        @media (max-width:$md3+px){
            width: 20rem;
            height: 20rem;
            filter: blur(12rem);
        }
    }
    .blob-1 {
        top: 0;
        right: 0;
    }

    .blob-2 {
        bottom: 20%;
        left: 0;
    }
</style>